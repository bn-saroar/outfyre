<footer class="bg-gray-900 text-white pt-16 pb-8">
      <div class="container mx-auto px-4">
        <div class="grid grid-cols-1 md:grid-cols-4 gap-8 mb-12">
          <div>
            <a
              href="#"
              class="font-['Pacifico'] text-2xl text-white mb-6 inline-block"
              >Outfyre</a
            >
            <p class="text-gray-400 mb-6">
              Your one-stop destination for modern fashion. Discover the latest
              trends in clothing, accessories, and footwear.
            </p>
            <div class="flex space-x-4">
              <a
                href="#"
                class="w-10 h-10 flex items-center justify-center bg-gray-800 rounded-full hover:bg-primary transition-colors"
              >
                <i class="ri-facebook-fill"></i>
              </a>
              <a
                href="#"
                class="w-10 h-10 flex items-center justify-center bg-gray-800 rounded-full hover:bg-primary transition-colors"
              >
                <i class="ri-instagram-line"></i>
              </a>
              <a
                href="#"
                class="w-10 h-10 flex items-center justify-center bg-gray-800 rounded-full hover:bg-primary transition-colors"
              >
                <i class="ri-twitter-x-line"></i>
              </a>
              <a
                href="#"
                class="w-10 h-10 flex items-center justify-center bg-gray-800 rounded-full hover:bg-primary transition-colors"
              >
                <i class="ri-pinterest-line"></i>
              </a>
            </div>
          </div>
          <div>
            <h3 class="font-semibold text-lg mb-6">Shop</h3>
            <ul class="space-y-3">
              <li>
                <a
                  href="#"
                  class="text-gray-400 hover:text-white transition-colors"
                  >Women's Clothing</a
                >
              </li>
              <li>
                <a
                  href="#"
                  class="text-gray-400 hover:text-white transition-colors"
                  >Men's Clothing</a
                >
              </li>
              <li>
                <a
                  href="#"
                  class="text-gray-400 hover:text-white transition-colors"
                  >Kids' Clothing</a
                >
              </li>
              <li>
                <a
                  href="#"
                  class="text-gray-400 hover:text-white transition-colors"
                  >Accessories</a
                >
              </li>
              <li>
                <a
                  href="#"
                  class="text-gray-400 hover:text-white transition-colors"
                  >Footwear</a
                >
              </li>
              <li>
                <a
                  href="#"
                  class="text-gray-400 hover:text-white transition-colors"
                  >New Arrivals</a
                >
              </li>
              <li>
                <a
                  href="#"
                  class="text-gray-400 hover:text-white transition-colors"
                  >Sale</a
                >
              </li>
            </ul>
          </div>
          <div>
            <h3 class="font-semibold text-lg mb-6">Customer Service</h3>
            <ul class="space-y-3">
              <li>
                <a
                  href="#"
                  class="text-gray-400 hover:text-white transition-colors"
                  >Contact Us</a
                >
              </li>
              <li>
                <a
                  href="#"
                  class="text-gray-400 hover:text-white transition-colors"
                  >FAQs</a
                >
              </li>
              <li>
                <a
                  href="#"
                  class="text-gray-400 hover:text-white transition-colors"
                  >Shipping & Returns</a
                >
              </li>
              <li>
                <a
                  href="#"
                  class="text-gray-400 hover:text-white transition-colors"
                  >Size Guide</a
                >
              </li>
              <li>
                <a
                  href="#"
                  class="text-gray-400 hover:text-white transition-colors"
                  >Track Order</a
                >
              </li>
              <li>
                <a
                  href="#"
                  class="text-gray-400 hover:text-white transition-colors"
                  >Gift Cards</a
                >
              </li>
            </ul>
          </div>
          <div>
            <h3 class="font-semibold text-lg mb-6">About</h3>
            <ul class="space-y-3">
              <li>
                <a
                  href="#"
                  class="text-gray-400 hover:text-white transition-colors"
                  >Our Story</a
                >
              </li>
              <li>
                <a
                  href="#"
                  class="text-gray-400 hover:text-white transition-colors"
                  >Careers</a
                >
              </li>
              <li>
                <a
                  href="#"
                  class="text-gray-400 hover:text-white transition-colors"
                  >Corporate Responsibility</a
                >
              </li>
              <li>
                <a
                  href="#"
                  class="text-gray-400 hover:text-white transition-colors"
                  >Press</a
                >
              </li>
              <li>
                <a
                  href="#"
                  class="text-gray-400 hover:text-white transition-colors"
                  >Privacy Policy</a
                >
              </li>
              <li>
                <a
                  href="#"
                  class="text-gray-400 hover:text-white transition-colors"
                  >Terms & Conditions</a
                >
              </li>
            </ul>
          </div>
        </div>
        <div class="border-t border-gray-800 pt-8">
          <div class="flex flex-col md:flex-row justify-between items-center">
            <p class="text-gray-400 text-sm mb-4 md:mb-0">
              © 2025 Fashion Nova. All rights reserved.
            </p>
            <div class="flex items-center space-x-4">
              <span class="text-gray-400 text-sm">Payment Methods:</span>
              <div class="flex space-x-3">
                <i class="ri-visa-fill text-xl"></i>
                <i class="ri-mastercard-fill text-xl"></i>
                <i class="ri-paypal-fill text-xl"></i>
                <i class="ri-apple-fill text-xl"></i>
                <i class="ri-google-fill text-xl"></i>
              </div>
            </div>
          </div>
        </div>
      </div>
    </footer>

    <!-- Back to Top Button -->
    <button
      class="back-to-top fixed bottom-6 right-6 w-12 h-12 bg-primary text-white rounded-full shadow-lg flex items-center justify-center hover:bg-primary/90 transition-colors"
      id="back-to-top"
    >
      <i class="ri-arrow-up-line text-lg"></i>
    </button>

<style>
  .footer-link {
    color: #adb5bd;
    text-decoration: none;
    transition: color 0.3s ease;
  }
  .footer-link:hover {
    color: #0dcaf0; /* Bootstrap info color */
    text-decoration: underline;
  }
  input::placeholder {
    color: #6c757d;
  }
  .btn-info {
    background-color: #0dcaf0;
    border-color: #0dcaf0;
    transition: background-color 0.3s ease;
  }
  .btn-info:hover {
    background-color: #0bb7d3;
    border-color: #0bb7d3;
  }
</style>

<!-- Add Bootstrap Icons CDN for icons -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">



    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <!-- AOS JS -->
<script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
<script>
  AOS.init();
</script>

<script id="mobile-menu-script">
      document.addEventListener("DOMContentLoaded", function () {
        const mobileMenuBtn = document.getElementById("mobile-menu-btn");
        const mobileMenu = document.getElementById("mobile-menu");
        const mobileSearchBtn = document.getElementById("mobile-search-btn");
        const mobileSearch = document.getElementById("mobile-search");
        mobileMenuBtn.addEventListener("click", function () {
          mobileMenu.classList.toggle("hidden");
          if (!mobileSearch.classList.contains("hidden")) {
            mobileSearch.classList.add("hidden");
          }
        });
        mobileSearchBtn.addEventListener("click", function () {
          mobileSearch.classList.toggle("hidden");
          if (!mobileMenu.classList.contains("hidden")) {
            mobileMenu.classList.add("hidden");
          }
        });
        const toggleButtons = document.querySelectorAll("[data-toggle]");
        toggleButtons.forEach((button) => {
          button.addEventListener("click", function () {
            const targetId = this.getAttribute("data-toggle");
            const targetElement = document.getElementById(targetId);
            targetElement.classList.toggle("hidden");
            const icon = this.querySelector("i");
            if (icon.classList.contains("ri-arrow-down-s-line")) {
              icon.classList.remove("ri-arrow-down-s-line");
              icon.classList.add("ri-arrow-up-s-line");
            } else {
              icon.classList.remove("ri-arrow-up-s-line");
              icon.classList.add("ri-arrow-down-s-line");
            }
          });
        });
      });
    </script>
    <!-- Cart Panel Script -->
    <script id="cart-panel-script">
      document.addEventListener("DOMContentLoaded", function () {
        const cartToggle = document.getElementById("cart-toggle");
        const closeCart = document.getElementById("close-cart");
        const cartPanel = document.getElementById("cart-panel");
        cartToggle.addEventListener("click", function () {
          cartPanel.classList.add("open");
          document.body.style.overflow = "hidden";
        });
        closeCart.addEventListener("click", function () {
          cartPanel.classList.remove("open");
          document.body.style.overflow = "";
        });
        // Close cart when clicking outside
        document.addEventListener("click", function (event) {
          if (
            cartPanel.classList.contains("open") &&
            !cartPanel.contains(event.target) &&
            event.target !== cartToggle
          ) {
            cartPanel.classList.remove("open");
            document.body.style.overflow = "";
          }
        });
      });
    </script>
    <!-- Featured Products Filter Script -->
    <script id="featured-filter-script">
      document.addEventListener("DOMContentLoaded", function () {
        const filterButtons = {
          all: document.getElementById("filter-all-btn"),
          women: document.getElementById("filter-women-btn"),
          men: document.getElementById("filter-men-btn"),
          kids: document.getElementById("filter-kids-btn"),
        };
        const productCards = document.querySelectorAll(".product-card");
        function updateButtonStates(activeButton) {
          Object.values(filterButtons).forEach((button) => {
            button.classList.remove("bg-primary", "text-white");
            button.classList.add("text-gray-700", "hover:bg-gray-200");
          });
          activeButton.classList.remove("text-gray-700", "hover:bg-gray-200");
          activeButton.classList.add("bg-primary", "text-white");
        }
        function filterProducts(category) {
          productCards.forEach((card) => {
            const productCategory = card
              .querySelector(".text-gray-500")
              .textContent.toLowerCase();
            if (
              category === "all" ||
              productCategory.includes(category.toLowerCase())
            ) {
              card.style.display = "";
            } else {
              card.style.display = "none";
            }
          });
        }
        filterButtons.all.addEventListener("click", function () {
          updateButtonStates(this);
          filterProducts("all");
        });
        filterButtons.women.addEventListener("click", function () {
          updateButtonStates(this);
          filterProducts("women's");
        });
        filterButtons.men.addEventListener("click", function () {
          updateButtonStates(this);
          filterProducts("men's");
        });
        filterButtons.kids.addEventListener("click", function () {
          updateButtonStates(this);
          filterProducts("kids");
        });
      });
    </script>
    <!-- Sort Dropdown Script -->
    <script id="sort-dropdown-script">
      document.addEventListener("DOMContentLoaded", function () {
        const sortButton = document.getElementById("sort-button");
        const sortDropdown = document.getElementById("sort-dropdown");
        if (sortButton && sortDropdown) {
          sortButton.addEventListener("click", function () {
            sortDropdown.classList.toggle("hidden");
          });
          document.addEventListener("click", function (event) {
            if (
              !sortButton.contains(event.target) &&
              !sortDropdown.contains(event.target)
            ) {
              sortDropdown.classList.add("hidden");
            }
          });
          const sortOptions = sortDropdown.querySelectorAll("button");
          sortOptions.forEach((option) => {
            option.addEventListener("click", function () {
              sortButton.querySelector("span").textContent = this.textContent;
              sortDropdown.classList.add("hidden");
            });
          });
        }
      });
    </script>
    <!-- Price Range Script -->
    <script id="filter-script">
      document.addEventListener("DOMContentLoaded", function () {
        const priceRange = document.getElementById("price-range");
        const priceValue = document.getElementById("price-value");
        const applyFiltersBtn = document.getElementById("apply-filters-btn");
        const loadingIndicator = document.getElementById("loading-indicator");
        const noResults = document.getElementById("no-results");
        const toast = document.createElement("div");
        toast.className =
          "fixed top-24 left-1/2 transform -translate-x-1/2 bg-green-500 text-white px-6 py-3 rounded-lg shadow-lg z-50 opacity-0 transition-opacity duration-300";
        toast.textContent = "";
        document.body.appendChild(toast);
        function showToast() {
          toast.style.opacity = "1";
          setTimeout(() => {
            toast.style.opacity = "0";
          }, 2000);
        }
        const clearFiltersBtn = document.getElementById("clear-filters-btn");
        const productsGrid = document.querySelector(
          ".grid.grid-cols-1.sm\\:grid-cols-2.lg\\:grid-cols-3.gap-6",
        );
        const filterAll = document.getElementById("filter-all");
        const filterWomens = document.getElementById("filter-womens");
        const savedFilterWomens = localStorage.getItem("filterWomens") === "true";
        const savedFilterMens = localStorage.getItem("filterMens") === "true";
        const savedFilterAll = localStorage.getItem("filterAll") === "true";
        if (savedFilterWomens || savedFilterMens || savedFilterAll) {
          filterWomens.checked = savedFilterWomens;
          filterMens.checked = savedFilterMens;
          filterAll.checked = savedFilterAll;
          filterProducts();
        }
        filterAll.addEventListener("change", handleCategoryFilter);
        filterWomens.addEventListener("change", handleCategoryFilter);
        filterMens.addEventListener("change", handleCategoryFilter);
        if (priceRange && priceValue) {
          priceRange.addEventListener("input", function () {
            priceValue.textContent = this.value;
          });
        }
        function getSelectedFilters() {
          const filters = {
            categories: Array.from(
              document.querySelectorAll('input[type="checkbox"]:checked'),
            ).map((cb) => cb.nextElementSibling.nextElementSibling.textContent),
            priceRange: parseInt(document.getElementById("price-range").value),
            color: document
              .querySelector('input[name="color"]:checked')
              ?.parentElement.querySelector(".color-circle").style.backgroundColor,
            size: document
              .querySelector('input[name="size"]:checked')
              ?.parentElement.querySelector(".size-box").textContent,
            rating:
              parseInt(
                document
                  .querySelector('input[name="rating"]:checked')
                  ?.closest("label")
                  .querySelectorAll(".ri-star-fill").length,
              ) || 0,
            showWomensOnly: document.getElementById("filter-womens").checked,
            showMensOnly: document.getElementById("filter-mens").checked,
          };
          return filters;
        }
        function handleCategoryFilter(e) {
          const filterAll = document.getElementById("filter-all");
          const filterWomens = document.getElementById("filter-womens");
          const filterMens = document.getElementById("filter-mens");
          if (e.target.id === "filter-all" && e.target.checked) {
            filterWomens.checked = false;
            filterMens.checked = false;
          } else if (e.target.id === "filter-womens" && e.target.checked) {
            filterAll.checked = false;
            filterMens.checked = false;
          } else if (e.target.id === "filter-mens" && e.target.checked) {
            filterAll.checked = false;
            filterWomens.checked = false;
          }
          localStorage.setItem("filterWomens", filterWomens.checked);
          localStorage.setItem("filterMens", filterMens.checked);
          localStorage.setItem("filterAll", filterAll.checked);
          filterProducts();
        }
        function filterProducts() {
          const filters = getSelectedFilters();
          const loadingIndicator = document.getElementById("loading-indicator");
          const noResults = document.getElementById("no-results");
          const productsGrid = document.querySelector(
            ".grid.grid-cols-1.sm\\:grid-cols-2.lg\\:grid-cols-3.gap-6",
          );
          loadingIndicator.classList.remove("hidden");
          showToast();
          setTimeout(() => {
            const products = Array.from(productsGrid.children);
            const filteredProducts = products.filter((product) => {
              const price = parseFloat(
                product.querySelector(".font-semibold").textContent.replace("$", ""),
              );
              const category = product.querySelector(".text-gray-500").textContent;
              const isWomensProduct = category.includes("Women's");
              const isMensProduct = category.includes("Men's");
              const productRating = product.querySelectorAll(".ri-star-fill").length;
              const productColor =
                product.querySelector(".color-circle")?.style.backgroundColor;
              if (filters.showWomensOnly && !isWomensProduct) return false;
              if (filters.showMensOnly && !isMensProduct) return false;
              if (price > filters.priceRange) return false;
              if (filters.rating && productRating < filters.rating) return false;
              if (filters.color && productColor !== filters.color) return false;
              if (filters.size) {
                const availableSizes = Array.from(
                  product.querySelectorAll(".size-box"),
                ).map((size) => size.textContent);
                if (!availableSizes.includes(filters.size)) return false;
              }
              return true;
            });
            loadingIndicator.classList.add("hidden");
            if (filteredProducts.length === 0) {
              productsGrid.classList.add("hidden");
              noResults.classList.remove("hidden");
            } else {
              productsGrid.classList.remove("hidden");
              noResults.classList.add("hidden");
              products.forEach((product) => product.classList.add("hidden"));
              filteredProducts.forEach((product) =>
                product.classList.remove("hidden"),
              );
            }
            const resultsSection =
              document.getElementById("no-results").parentElement;
            resultsSection.scrollIntoView({ behavior: "smooth", block: "start" });
          }, 1000);
        }
        function clearFilters() {
          document
            .querySelectorAll('input[type="checkbox"]')
            .forEach((cb) => (cb.checked = false));
          document.getElementById("filter-all").checked = true;
          localStorage.setItem("filterWomens", "false");
          localStorage.setItem("filterMens", "false");
          localStorage.setItem("filterAll", "true");
          priceRange.value = 250;
          priceValue.textContent = "250";
          document.querySelector('input[name="color"]').checked = true;
          document.querySelector('input[name="size"]').checked = true;
          document.querySelector('input[name="rating"]').checked = true;
          noResults.classList.add("hidden");
          productsGrid.classList.remove("hidden");
          Array.from(productsGrid.children).forEach((product) => {
            product.classList.remove("hidden");
          });
        }
        if (applyFiltersBtn) {
          applyFiltersBtn.addEventListener("click", filterProducts);
        }
        if (clearFiltersBtn) {
          clearFiltersBtn.addEventListener("click", clearFilters);
        }
      });
    </script>
    <!-- Back to Top Script -->
    <script id="back-to-top-script">
      document.addEventListener("DOMContentLoaded", function () {
        const backToTopButton = document.getElementById("back-to-top");
        window.addEventListener("scroll", function () {
          if (window.pageYOffset > 300) {
            backToTopButton.classList.add("visible");
          } else {
            backToTopButton.classList.remove("visible");
          }
        });
        backToTopButton.addEventListener("click", function () {
          window.scrollTo({
            top: 0,
            behavior: "smooth",
          });
        });
      });
    </script>
  </body>
</html>
